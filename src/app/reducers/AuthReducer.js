const initialState = {
  logged    : false,
  isAdmin   : false,
  appProfile: {
    author : "Huynh Minh Tu",
    address: "https://mrhmt.com",
  },
};

export default (state = initialState) => {
  return state;
};
